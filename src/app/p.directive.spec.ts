import { PDirective } from './p.directive';

describe('PDirective', () => {
  it('should create an instance', () => {
    const directive = new PDirective();
    expect(directive).toBeTruthy();
  });
});
